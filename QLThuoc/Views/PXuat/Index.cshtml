@model IEnumerable<QLThuoc.Models.PXUAT>

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-md-10">
                <strong class="text-uppercase text-dark">Xuất thuốc</strong>
            </div>
            <div class="col-md-2 text-end">
                <a href="@Url.Action("Index","CTPXUAT")" class="btn btn-sm btn-success">Thuốc đã xuất </a>
                <a href="@Url.Action("Create","PXUAT")" class="btn btn-sm btn-primary">Tạo mới </a>
            </div>
        </div>
    </div>
    <div class="card-body p-3">
        <table class="table table-secondary table-bordered">
            <thead>
                <tr>
                    <th style="width:25%" class="text-center">Mã phiếu</th>
                    <th style="width:25%" class="text-center">Ngày xuất</th>
                    <th style="width:25%" class="text-center">Tên nhân viên</th>
                    <th style="width:25%" class="text-center">Chức năng</th>
                </tr>
            </thead>
            <tbody style="width:auto" class="text-center">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.maphieu)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ngayxuat)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NHANVIEN.tennv)
                        </td>
                        <td class="text-center">
                            <a href="@Url.Action("Edit","PXuat", new { id = item.maphieu })" class="btn btn-sm btn-warning">Sửa </a>
                            <a href="@Url.Action("Index","CTPXuat", new { id = item.maphieu }, null)" class="btn btn-sm btn-secondary">Xem </a>
                            <a href="@Url.Action("Delete","PXuat", new { id = item.maphieu })" class="btn btn-sm btn-danger">Xóa </a>
                            <a href="@Url.Action("PrintPDF","CTPXuat", new { id = item.maphieu }, null)" class="btn btn-sm btn-success">PDF</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
